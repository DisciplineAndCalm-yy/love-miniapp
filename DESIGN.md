# 我们 · 情侣小程序设计

面向一对情侣的私密空间。数据全部走微信云开发（云数据库 + 云存储 + 云函数），默认只有绑定成功的两个人能读写。

## 产品原则

- 两个人一份数据，不做成社交产品。
- 首页回答三件事：在一起几天、今天打卡了没、下一个纪念日还有多久。
- 打卡要轻：心情 + 一句话 + 可选一张图，30 秒完成。
- 空间是共同博客，不是朋友圈；默认只有对方看得到。

## 信息架构

```
首页        今日状态、在一起天数、下一纪念日、快捷入口
打卡        今日心情打卡、双方卡片、连续天数
日历        月视图（打卡点 / 纪念日）、当日详情
空间        共同博客列表 → 写一篇 / 看详情
我们        绑定情侣、纪念日管理、资料
```

纪念日从「我们」进入，首页和日历只做展示与倒计时。

## 核心流程

### 1. 绑定

1. A 打开「我们」→ 生成 6 位邀请码。
2. B 输入邀请码加入。两人写入同一个 `couples` 文档。
3. 绑定后，打卡 / 空间 / 日历 / 纪念日都挂在 `coupleId` 上。

未绑定也可以先体验打卡草稿（仅本地提示去绑定），真正写入云库需要 `coupleId`。

### 2. 日常打卡

- 每天每人一条，键为 `dateKey + _openid`。
- 心情：爱你 / 想你 / 平静 / 忙碌 / 难过。
- 对方当天有记录时，首页和打卡页立刻出现对方卡片。
- 连续天数：我的连续、对方连续、两人同一天打卡的连续。今天还没打也不立刻清零（算到昨天）。

### 3. 博客空间

- 标题 + 正文 + 最多 9 张图。
- 列表按时间倒序，详情可删自己的文章。

### 4. 日历与纪念日

- 日历格子：粉点 = 有人打卡，金点 = 纪念日。
- 纪念日支持一次性或每年重复，首页展示最近的一次。
- 纪念日支持短文案和主题色，方便做成「卡片」。

## 云数据库

### users

| 字段 | 说明 |
| --- | --- |
| `_openid` | 自动 |
| `nickName` | 展示名 |
| `avatarUrl` | 头像 fileID 或微信头像 |
| `coupleId` | 绑定后写入 |
| `remindEnabled` | 是否打开纪念日订阅提醒 |
| `createdAt` | 时间戳 |

### couples

| 字段 | 说明 |
| --- | --- |
| `inviteCode` | 6 位大写邀请码 |
| `memberOpenids` | 1～2 个 openid |
| `togetherSince` | 在一起的起始日 `YYYY-MM-DD` |
| `status` | `pending` / `paired` |
| `createdAt` | 时间戳 |

### checkins

| 字段 | 说明 |
| --- | --- |
| `_openid` | 打卡人 |
| `coupleId` | 情侣空间 |
| `dateKey` | `YYYY-MM-DD` |
| `mood` | `love` `miss` `calm` `busy` `sad` |
| `content` | 一句话 |
| `photo` | 云存储 fileID，可选 |
| `createdAt` | 时间戳 |

唯一建议：`coupleId + dateKey + _openid`。

### posts

| 字段 | 说明 |
| --- | --- |
| `_openid` | 作者 |
| `coupleId` | |
| `title` | |
| `content` | |
| `photos` | fileID 数组 |
| `createdAt` / `updatedAt` | |

### anniversaries

| 字段 | 说明 |
| --- | --- |
| `_openid` | 创建人 |
| `coupleId` | |
| `title` | 如「第一次见面」 |
| `date` | `YYYY-MM-DD` |
| `repeatYearly` | boolean |
| `emoji` | |
| `color` | 卡片色 |
| `note` | 想对对方说的话 |
| `createdAt` | |

### moments

| 字段 | 说明 |
| --- | --- |
| `_openid` | 作者 |
| `coupleId` | |
| `doing` | 标签（吃饭/午睡/…，最长 8 字） |
| `content` | 正文（最长 500 字） |
| `photos` | fileID 数组，最多 6 张 |
| `createdAt` / `updatedAt` | |

### pets

一对情侣共用一只。`species / name / level / exp / hunger / mood / clean / energy / state / updatedAt`。
饥饿与心情随时间线性衰减（每分钟 0.033），喂食 / 陪玩 / 洗澡 / 哄睡恢复并加经验，满经验升级。

### adventures

双人冒险存档，一对情侣一份（无历史留档，重新开局即覆盖）。

| 字段 | 说明 |
| --- | --- |
| `_openid` | 开局人 |
| `coupleId` | |
| `storyId` | `forest` / `sea` / `star` |
| `mode` | `solo` / `duo` |
| `node` | 当前剧情节点 |
| `hp` / `maxHp` / `atk` / `gold` | 三维属性（`atk` 为事件加成，装备另算） |
| `items` | 装备/道具数组 `{ name, atk, at, by }`，`atk` 是攻击加成。**总攻击 = atk + Σ 装备攻击** |
| `battle` | 当前战斗对象；为空时以 `{}` 表示（见下方「云库三铁律」） |
| `journey` | 旅途进度 `{ total, left, exit }` |
| `trail` | 当前遭遇 `{ depth, total, kind, a, b, battle, hp, gold }`，`kind` 为 `event`/`battle` |
| `flags` / `log` / `endings` | 已走过的选择 / 足迹日志 / 已达成的结局 |
| `partnerJoined` | 双人局里对方是否已加入 |
| `createdAt` / `updatedAt` | |

**玩法结构（Roguelike 式，非固定脚本）**

```
主线（3~6 次选择 + 1 场 BOSS）
   └─ 旅途（10~16 连难，每难随机）
         ├─ 45% 遭遇战：本主题 10 种怪随机，血量/攻击随层数 ×(1 + 0.18·层)
         ├─ 42% 奇遇事件：本主题 30 组剧本随机，各 2 个选项、不同结算
         └─ 13% 安营福利难：回血，自动跳过不占操作
   └─ 终章结局（每条线多个结局）
```

- 每局**不重复**：当前主题的 30 组事件与 10 种怪都记在 `journey.usedEvents / usedFoes` 里，同一局出现过的不会再抽到，用光了才重置。
- 主题专属：三个主题各有一套 30 组奇遇（雾林 / 深海 / 星夜），按 `journey.theme` 取池，画风与叙事各不相同。
- 战斗为回合制：`attack` / `defend`（受伤减半）/ `flee`（50% 逃脱）。宠物心情 ≥60 时每步助战回血、攻击 +2。
- 结局不会因走到出口就丢：出站时若出口是结局节点，服务端直接记档并回传结局名，前端弹结局弹窗。

## 云库三铁律（踩过的坑，务必遵守）

1. **不可 `set` 系统字段**：`_openid` 等由云库托管，写入时只能靠 `add` 自动带，或在 `update` 里合并业务字段。
2. **`null` 上不能长对象**：若字段当前是 `null`，直接 `update` 成对象会报 `-502001`（`Cannot create field … in element {x: null}`）。空状态一律用 `{}`，判断时用 `x && x.id` 而非 `if (x)`。
3. **`undefined` 会让整单失败**：写库前先 `JSON.parse(JSON.stringify(patch))` 洗掉所有 `undefined`。

## 权限与鉴权

- 小程序端**只读**，所有写入走 `api` 云函数；函数内一律 `requireCouple` + `_openid` 校验。
- 服务端只认「是否空间成员」，因此**单人沙盒**（见下）能走通全部流程。

## 单人体验码

`bindCouple` 内置固定体验码 **`DEMO01` ~ `DEMO10`**（含数字 `0/1`，而随机邀请码字符集是 `ABCDEFGHJKLMNPQRSTUVWXYZ23456789`，不含 `0/1`，故两者**永不冲突**）。

- 输入体验码不会加入别人的空间，而是**新建一个独立空间**（`couples.isDemo = true`），不同用户用同一个码也各得一份互不可见的沙盒。
- 已绑定真实空间的人输入体验码会被拒绝（保护真实数据）。
- 清理：按 `isDemo == true` 过滤 `couples` 删除即可，不影响真实数据。

## 已具备（部署后可用）

- 邀请码绑定 / 退出空间 / 单人体验码
- 头像与昵称、对方资料展示
- 日常打卡、连续天数、日历叠加
- 共同博客、日常动态（含删图、删帖清云存储）
- 悄悄话聊天（未读红点 / 已读回执 / 撤回）
- 云养宠物（喂食/陪玩/洗澡/哄睡/升级）
- 纪念日卡片、倒计时与订阅提醒
- 7 个互动玩法：扭蛋机 / 默契考验 / 心愿瓶 / 掷骰子 / 真心话 / 晚安打卡 / 双人冒险
- 双人冒险：3 条故事线、每条 30 组专属奇遇事件（共 90 组）+ 10 种专属怪物（共 30 种）、回合制战斗、装备掉落、宠物助战、多结局

## 后续可加

- 冒险：第四/第五条故事线、成就图鉴、结局收集册
- 共同待办 / 心愿清单
- 年度回忆册（把打卡和博客自动排版）
