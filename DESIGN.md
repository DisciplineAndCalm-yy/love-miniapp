# 我们 · 情侣小程序设计

面向一对情侣的私密空间。数据全部走微信云开发（云数据库 + 云存储 + 云函数），默认只有绑定成功的两个人能读写。

## 产品原则

- 两个人一份数据，不做成社交产品。
- 首页回答三件事：在一起几天、今天打卡了没、下一个纪念日还有多久。
- 打卡要轻：心情 + 一句话 + 可选一张图，30 秒完成。
- 空间是共同博客，不是朋友圈；默认只有对方看得到。

## 信息架构

```
首页        今日状态、在一起天数、下一纪念日、快捷入口
打卡        今日心情打卡、双方卡片、连续天数
日历        月视图（打卡点 / 纪念日）、当日详情
空间        共同博客列表 → 写一篇 / 看详情
我们        绑定情侣、纪念日管理、资料
```

纪念日从「我们」进入，首页和日历只做展示与倒计时。

## 核心流程

### 1. 绑定

1. A 打开「我们」→ 生成 6 位邀请码。
2. B 输入邀请码加入。两人写入同一个 `couples` 文档。
3. 绑定后，打卡 / 空间 / 日历 / 纪念日都挂在 `coupleId` 上。

未绑定也可以先体验打卡草稿（仅本地提示去绑定），真正写入云库需要 `coupleId`。

### 2. 日常打卡

- 每天每人一条，键为 `dateKey + _openid`。
- 心情：爱你 / 想你 / 平静 / 忙碌 / 难过。
- 对方当天有记录时，首页和打卡页立刻出现对方卡片。
- 连续天数：我的连续、对方连续、两人同一天打卡的连续。今天还没打也不立刻清零（算到昨天）。

### 3. 博客空间

- 标题 + 正文 + 最多 9 张图。
- 列表按时间倒序，详情可删自己的文章。

### 4. 日历与纪念日

- 日历格子：粉点 = 有人打卡，金点 = 纪念日。
- 纪念日支持一次性或每年重复，首页展示最近的一次。
- 纪念日支持短文案和主题色，方便做成「卡片」。

## 云数据库

### users

| 字段 | 说明 |
| --- | --- |
| `_openid` | 自动 |
| `nickName` | 展示名 |
| `avatarUrl` | 头像 fileID 或微信头像 |
| `coupleId` | 绑定后写入 |
| `remindEnabled` | 是否打开纪念日订阅提醒 |
| `createdAt` | 时间戳 |

### couples

| 字段 | 说明 |
| --- | --- |
| `inviteCode` | 6 位大写邀请码 |
| `memberOpenids` | 1～2 个 openid |
| `togetherSince` | 在一起的起始日 `YYYY-MM-DD` |
| `status` | `pending` / `paired` |
| `createdAt` | 时间戳 |

### checkins

| 字段 | 说明 |
| --- | --- |
| `_openid` | 打卡人 |
| `coupleId` | 情侣空间 |
| `dateKey` | `YYYY-MM-DD` |
| `mood` | `love` `miss` `calm` `busy` `sad` |
| `content` | 一句话 |
| `photo` | 云存储 fileID，可选 |
| `createdAt` | 时间戳 |

唯一建议：`coupleId + dateKey + _openid`。

### posts

| 字段 | 说明 |
| --- | --- |
| `_openid` | 作者 |
| `coupleId` | |
| `title` | |
| `content` | |
| `photos` | fileID 数组 |
| `createdAt` / `updatedAt` | |

### anniversaries

| 字段 | 说明 |
| --- | --- |
| `_openid` | 创建人 |
| `coupleId` | |
| `title` | 如「第一次见面」 |
| `date` | `YYYY-MM-DD` |
| `repeatYearly` | boolean |
| `emoji` | |
| `color` | 卡片色 |
| `note` | 想对对方说的话 |
| `createdAt` | |

## 权限（思路）

- `users`：仅本人读写。
- `couples`：成员可读；创建走云函数 `bindCouple`，避免邀请码被扫库。
- `checkins` / `posts` / `anniversaries`：`coupleId` 对应情侣的成员可读写，创建时必须带自己的 `_openid`。

生产环境建议把「加入情侣、删除空间」全部放进云函数，客户端只读。

## 已具备（部署后可用）

- 邀请码绑定 / 退出空间
- 头像与昵称、对方资料展示
- 日常打卡、连续天数、日历叠加
- 共同博客（含删图、删帖清云存储）
- 纪念日卡片与倒计时
- 纪念日订阅提醒（需自行填模板 ID）

## 后续可加

- 共同待办 / 心愿清单
- 悄悄话（阅后对方已读）
- 年度回忆册（把打卡和博客自动排版）
